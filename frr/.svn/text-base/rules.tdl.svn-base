;;; -*- Mode: TDL; Coding: utf-8 -*-

; woodley: turned off the narrow focus rules, since they seem to give MRSes that are strictly subsumed by the regular rules.
; nf-comp-head := nf-comp-head-phrase.
; nf-subj-head := nf-subj-head-phrase.

context1-decl-head-opt-subj := context1-decl-head-opt-subj-phrase.

; Rule instances for head-modifier structures. Corresponding types
; are defined in matrix.tdl.  The matrix customization script did
; not need to add any further constraints, so no corresponding types
; appear in frisian.tdl

adj-head-int := ordinary-adj-head-int-phrase.
head-adj-int := ordinary-head-adj-int-phrase.
adj-head-scop := ordinary-adj-head-scop-phrase.
head-adj-scop := ordinary-head-adj-scop-phrase.

forfelt-adj-head-int := forfelt-adj-head-int-phrase.
forfelt-adj-head-scop := forfelt-adj-head-scop-phrase.

head-spec := head-spec-phrase.

head-subj := head-subj-phrase.
subj-head := subj-head-phrase.
subj-head-emb := subj-head-phrase-emb.

head-comp := head-comp-phrase.
comp-head := comp-head-phrase.
head-comp-2 := head-comp-phrase-2.
comp-head-2 := comp-head-phrase-2.
comp-head-emb := comp-head-phrase-emb.

head-sc-comp := head-sc-comp-phrase.
sc-comp-head := sc-comp-head-phrase.
head-sc-comp-2 := head-sc-comp-phrase-2.
sc-comp-head-2 := sc-comp-head-phrase-2.
sc-comp-head-emb := sc-comp-head-phrase-emb.


bare-np := bare-np-phrase.

n1-top-coord := n1-top-coord-rule.

n1-mid-coord := n1-mid-coord-rule.

n1-bottom-coord := n1-bottom-coord-rule.

np1-top-coord := np1-top-coord-rule.

np1-mid-coord := np1-mid-coord-rule.

np1-bottom-coord := np1-bottom-coord-rule.

vp1-top-coord := vp1-top-coord-rule.

vp1-mid-coord := vp1-mid-coord-rule.

vp1-bottom-coord := vp1-bottom-coord-rule.

s1-top-coord := s1-top-coord-rule.

s1-mid-coord := s1-mid-coord-rule.

s1-bottom-coord := s1-bottom-coord-rule.

int := int-cl.

xsubj := extracted-subj-phrase.
rc := relative-clause.
